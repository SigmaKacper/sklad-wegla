#include <iostream>

int liczba_kwadrat(int & liczba, t[i])
{
	return liczba * liczba;
}
int main()
{
	
  int liczba;
  std::cout<<"Podaj liczbe :";
  std::cin>>liczba;
  
  int tabela[], ile;
  std::cout<<"Ile razy ma spotengowac :";
  cin>>ile;
  tabela[]=tabela[ile];
  
  std::cout<<liczba_kwadrat(liczba, tabela[ile]);
}
